# Plugin metadata
plugin.id=com.gluecron.jetbrains
plugin.version=0.1.0

# IntelliJ Platform version to build against (2023.1)
platform.version=2023.1

# Gradle settings
org.gradle.caching=true
org.gradle.parallel=true
kotlin.incremental=true
